[build]
  command = "npm install && npm run build"
  publish = "dist"
  environment = { SECRETS_SCAN_ENABLED = "false" }

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
